# Audio ingest & spectrogram processing
librosa>=0.10
soundfile>=0.12
numpy>=1.24
requests>=2.31

# Training / inference
# For CPU-only installs: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
torchvision>=0.15
Pillow>=10.0
